# MS Fabric Starter Kit

Initial starter template for building apps with Microsoft Fabric.

## Current Scope

- Basic Next.js app structure
- Home page scaffold in `app/page.tsx`
- UI component setup with `@/components/ui/button`

## Run Locally

```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000`.